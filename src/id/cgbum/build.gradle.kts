plugins {
    alias(kei.plugins.extension)
}

keiyoushi {
    name = "Cgbum"
    versionCode = 79
    contentWarning = ContentWarning.SAFE
    libVersion = "1.4"

    source {
        lang = "id"
        baseUrl = "https://cgbum.com"
    }
}
